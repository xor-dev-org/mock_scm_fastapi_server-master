openapi: 3.0.0
info:
  title: SCM Procurement Mock Server
  version: 1.0.0
paths:
  /auth/msal/login:
    post:
      summary: Mock MSAL Login
  /auth/supplier/signup:
    post:
      summary: Supplier Signup
  /auth/supplier/login:
    post:
      summary: Supplier Login
  /po:
    get:
      summary: Get Purchase Orders
    post:
      summary: Create Purchase Order
  /po/{po_id}:
    get:
      summary: Get PO by ID
    put:
      summary: Update PO
  /chat/sessions:
    get:
      summary: List chat sessions for current user
    post:
      summary: Create chat session
  /chat/sessions/search:
    get:
      summary: Search chat sessions and suggestions
  /chat/sessions/search-or-create:
    post:
      summary: Search existing session or create a new one
  /chat/sessions/{session_id}/messages:
    get:
      summary: Get paginated messages for a session
    post:
      summary: Send message in chat session
  /chat/sessions/{session_id}/read:
    post:
      summary: Mark session as read
  /chat/realtime/bootstrap:
    get:
      summary: Get realtime connection bootstrap metadata
  /chat/realtime/events:
    post:
      summary: Event Grid ingestion endpoint for ACS chat events